<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application>
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <activity
            android:name=".ui.activities.camera.TruvideoSdkCameraActivity"
            android:exported="false"
            android:enableOnBackInvokedCallback="true"
            android:label="@string/title_activity_camera_compose"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".ui.activities.refactor.camera.TruvideoSdkNewCameraActivity"
            android:exported="false"
            android:enableOnBackInvokedCallback="true"
            android:label="@string/title_activity_camera_compose"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".ui.activities.arcamera.TruvideoSdkArCameraActivity"
            android:exported="false"
            android:enableOnBackInvokedCallback="true"
            android:label="@string/title_activity_camera_compose"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".ui.activities.scanner.TruvideoSdkCameraScannerActivity"
            android:exported="false"
            android:enableOnBackInvokedCallback="true"
            android:label="@string/title_activity_camera_compose"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.truvideo.sdk.camera.TruvideoSdkCameraInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>